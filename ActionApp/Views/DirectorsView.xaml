<UserControl x:Class="ActionApp.Views.DirectorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ActionApp.ViewModels"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:DirectorsViewModel />
    </UserControl.DataContext>

    <Grid Background="{StaticResource LightBgBrush}" Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page title -->
        <TextBlock Grid.Row="0" Text="Directors" Style="{StaticResource PageTitleStyle}"/>

        <!-- Search bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Grid>
                <TextBox Width="400"
                         Style="{StaticResource SearchBoxStyle}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Search..." Style="{StaticResource PlaceholderStyle}">
                    <TextBlock.Visibility>
                        <Binding Path="SearchText" Converter="{x:Null}" FallbackValue="Visible"/>
                    </TextBlock.Visibility>
                </TextBlock>
            </Grid>
        </StackPanel>

        <!-- Data grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding DirectorsView}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Director" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Birth Year" Binding="{Binding BirthYear}" Width="100"/>
                <DataGridTextColumn Header="Movies" Binding="{Binding MovieCount}" Width="80"/>
                <DataGridTextColumn Header="Top Movie" Binding="{Binding TopMovie}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>